{% extends 'layout.html.twig' %}


{% block body %}

    <h1>HeureProf</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ heure_prof.id }}</td>
            </tr>
            <tr>
                <th>Nbreheure</th>
                <td>{{ heure_prof.nbreheure }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ heure_prof.mois }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('heure_prof_index') }}">back to list</a>

    <a href="{{ path('heure_prof_edit', {'id': heure_prof.id}) }}">edit</a>

    {{ include('heure_prof/_delete_form.html.twig') }}
{% endblock %}
