{% extends 'layout.html.twig' %}


{% block body %}

    <h1>Create new HeureProf</h1>

    {{ include('heure_prof/_form.html.twig') }}

    <a href="{{ path('heure_prof_index') }}">back to list</a>
{% endblock %}
