{% extends 'layout.html.twig' %}


{% block body %}

    <h1>Edit HeureProf</h1>

    {{ include('heure_prof/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('heure_prof_index') }}">back to list</a>

    {{ include('heure_prof/_delete_form.html.twig') }}
{% endblock %}
