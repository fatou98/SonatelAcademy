<form method="post" action="{{ path('retard_delete', {'id': retard.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retard.id) }}">
    <button class="btn">Supprimer</button>
</form>
