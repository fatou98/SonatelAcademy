{% extends 'layout.html.twig' %}


{% block body %} 
<div class="row card table-responsive" style="padding:30px;width:95%;">
    <h1>Liste étudiant</h1>
<br>
<hr style="text-align:center;">
    <table id="datatable-buttons" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Prenom</th>
                <th>Nom</th>
                <th>Dateofbirth</th>
                <th>Lieunaissance</th>
                <th>Image</th>
                <th>Numpiece</th>
                <th>MobileNumber</th>
                <th>Adresse</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etudiant in etudiants %}
            <tr>
                <td>{{ etudiant.prenom }}</td>
                <td>{{ etudiant.nom }}</td>
                <td>{{ etudiant.dateofbirth }}</td>
                <td>{{ etudiant.lieunaissance }}</td>
                <td><image src="data:image/png;base64,{{ etudiant.image }}" ></td>
                <td>{{ etudiant.numpiece }}</td>
                <td>{{ etudiant.mobileNumber }}</td>
                <td>{{ etudiant.adresse }}</td>
                <td>{{ etudiant.email }}</td>
                <td>
                    <a href="{{ path('etudiant_show', {'id': etudiant.id}) }}">Afficher</a>
                    <a href="{{ path('etudiant_edit', {'id': etudiant.id}) }}">Editer</a>
                        {{ include('etudiant/_delete_form.html.twig') }}

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
